

name: RecSync-env installation test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - run: sudo apt-get python-twisted python-requests python-simplejson python-urllib3 git make 
    - run: |
        make init
        make vars
     - run: |
        make install
        make exist

     
                
